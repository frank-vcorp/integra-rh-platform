# Checkpoint: Removed Manus code (2025-11-02 21:52)

Resumen
- Se eliminó la integración de Manus (OAuth/plugin/runtime/UI) para simplificar el stack a solo Firebase Auth.

Cambios
- Vite: se quitó `vite-plugin-manus-runtime` del config.
  - integra-rh-manus/vite.config.ts:1
- Server: se eliminaron SDK y rutas OAuth de Manus.
  - integra-rh-manus/server/_core/sdk.ts (deleted)
  - integra-rh-manus/server/_core/oauth.ts (deleted)
- Client: se removió el componente `ManusDialog` y `getLoginUrl` ahora siempre devuelve `/login`.
  - integra-rh-manus/client/src/components/ManusDialog.tsx (deleted)
  - integra-rh-manus/client/src/const.ts:1

Notas
- El login se realiza únicamente vía Firebase en `/login`.
- Si existen variables `OAUTH_*` o `VITE_APP_ID`, quedan ignoradas.

