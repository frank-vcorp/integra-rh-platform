# Checkpoint IMPL-20260520-01
**Fecha:** 2026-05-20  
**Agente:** SOFIA - Builder  
**SPEC de referencia:** SPEC-ARCH-20260520-06  
**ID de intervención:** IMPL-20260520-01

---

## Cambio aplicado

**Archivo modificado:** `cloudbuild.yaml` — Paso 6 (único paso modificado)

| Campo | Antes | Después |
|-------|-------|---------|
| Comentario del paso | `Deploy Firebase & Functions` | `Deploy Firebase Hosting + Functions (IMPL-20260520-01)` |
| Comando firebase deploy | `--only hosting` | `--only hosting,functions` |
| `npm install` en functions/ | Ausente | `cd functions && npm install --production && cd ..` |

**Diff conceptual:**
```diff
-        # Desplegar SOLO Hosting para evitar errores de permisos en Functions mientras se propagan
-        firebase deploy --project=$PROJECT_ID --only hosting
+        # Instalar dependencias de Functions en CI (node_modules no incluido en imagen)
+        cd functions && npm install --production && cd ..
+        # Desplegar Hosting y Functions heredadas en el mismo paso
+        firebase deploy --project=$PROJECT_ID --only hosting,functions
```

---

## Validación estática

- ✅ `python3 -c "import yaml; ..."` → `YAML OK — 6 pasos` (sin errores de parseo)
- ✅ Estructura de `firebase.json` confirmada: `functions.source = "functions"`, `runtime = "nodejs20"`, `v1: true`
- ✅ Rewrites de Hosting apuntan a `asignarPruebasPsicometricas`, `reenviarInvitacion`, `manageUserRole`, `listUsers`, `listUsersHttp` — todas resueltas en la misma imagen de funciones
- ✅ `FIREBASE_SA_KEY` ya estaba declarado en `availableSecrets` y `secretEnv` — sin cambios requeridos en IAM a nivel de YAML

---

## Riesgos operativos pendientes (a validar en primer build real)

| Riesgo | Probabilidad | Acción |
|--------|-------------|--------|
| SA de Cloud Build sin rol `Cloud Functions Admin` | Media — funciones activas sugieren que ya existía | Verificar: `gcloud projects get-iam-policy integra-rh --flatten="bindings[].members" --filter="bindings.members:serviceAccount:*@cloudbuild.gserviceaccount.com"` |
| `functions/node_modules` en `.gitignore` → `npm install` en CI puede tardar | Bajo — `npm install --production` ya incluido en el paso | Medir tiempo del build post-merge |
| Fallo en Functions bloquea Hosting | Esperado por diseño — pipeline falla completo para evitar drift | Si se decide separar, usar dos pasos distintos con `continue-on-error` explícito |
| `serviceAccountKey.json` en `functions/` en git | Alto riesgo de seguridad — DEBE verificarse que esté en `.gitignore` | `git ls-files functions/serviceAccountKey.json` — si retorna algo, eliminar del tracking inmediatamente |

---

## Criterios de aceptación alcanzados

- [x] Paso 6 incluye `--only hosting,functions` — Functions se publican en cada push a master
- [x] Sintaxis YAML válida (validación estática Python)
- [x] El resto del pipeline (pasos 1-5: Docker build/push/Cloud Run deploy) permanece intacto
- [ ] Build real en master finaliza `SUCCESS` ← pendiente de la próxima corrida del trigger
- [ ] Confirmación de permisos IAM de la SA ← pendiente de verificación manual

---

## Próximos pasos sugeridos

1. Verificar IAM de la SA de Cloud Build antes del primer push (ver comando en tabla de riesgos).
2. Confirmar que `functions/serviceAccountKey.json` NO está en git (riesgo de seguridad crítico).
3. Al completarse el primer build exitoso, actualizar PROYECTO.md marcando `[✓] ARCH-20260520-06`.
